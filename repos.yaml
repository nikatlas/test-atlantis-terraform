# repos.yaml
workflows:
  myworkflow:
    plan:
      steps:
      - init
      - run: tunnel.sh start
      - plan
      - run: tunnel.sh stop


repos:
  - id: /.*/
    workflow: myworkflow
    allow_custom_workflows: true
    allowed_overrides: [workflow]