# repos.yaml
workflows:
  myworkflow:
    plan:
      steps:
      - init
      - run: echo "here someone ran me"
repos:
  - id: /.*/
    workflow: myworkflow
    pre_workflow_hooks:
      - run: echo "PRE WORKFLOW"
projects:
- name: testing-atlantis-now-nik
  dir: test-project
  workflow: myworkflow
