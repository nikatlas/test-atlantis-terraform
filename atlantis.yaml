version: 3

workflows:
  myworkflow:
    plan:
      steps:
      - init
      - run: echo "here someone ran me"
      - run: ./scripts/another.sh start opkd
      - plan
      - run: ./scripts/another.sh stop


projects:
- name: testing-atlantis-now-nik
  dir: .
  workflow: myworkflow
