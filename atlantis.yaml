version: 3

workflows:
  myworkflow:
    plan:
      steps:
      - init
      - run: echo "here someone ran me"
      - run: ./scripts/another.sh


projects:
- name: testing-atlantis-now-nik
  dir: .
  workflow: myworkflow
