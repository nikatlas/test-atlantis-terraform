version: 3

workflows:
  myworkflow:
    plan:
      steps:
      - env:
          name: ENV_NAME
          command: "PROJECT=${PROJECT_NAME:-$(pwd)}; echo ${PROJECT##*/}"
      - init
      - run: echo "here someone ran me $ENV_NAME"
      - run: ../../../scripts/another.sh start
      - plan
      - run: ../../../scripts/another.sh stop


projects:
- name: testing-atlantis-now-nik
  dir: test-project/olx-pk/development
  workflow: myworkflow
