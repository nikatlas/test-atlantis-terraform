version: 3

workflows:
  another:
    plan:
      steps:
      - run: echo "ANOTHER TASK"

  myworkflow:
    plan:
      steps:
      - init
      - run: echo "here someone ran me"
      - run: env
      - another


projects:
- name: testing-atlantis-now-nik
  dir: .
  workflow: myworkflow
